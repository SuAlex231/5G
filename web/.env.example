# Frontend API base URL
VITE_API_BASE_URL=http://localhost:8000

# Notes:
# 1. Copy this file to .env in the web directory
# 2. To change the backend host/IP, update VITE_API_BASE_URL
# 3. After changing, rebuild the web container with: docker compose build web